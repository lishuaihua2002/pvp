# 网页横版1v1 PVP格斗游戏开发Prompt

你是一名资深网页游戏开发工程师、全栈工程师和游戏交互设计师。请为我开发一个可运行、可部署的网页横版1v1 PVP格斗游戏。

不要只生成静态页面或产品原型，必须实现完整的注册登录、照片角色制作、实时匹配、双人联机格斗、退出同步、音效和Supabase数据存储功能。

请优先保证：

1. 项目可以实际运行；
2. 格斗操作响应迅速；
3. 联机状态稳定；
4. 照片角色制作流程简单；
5. 打击反馈明显、有趣；
6. 代码结构便于后续增加技能、地图和角色动作；
7. 不依赖Supabase以外的自建后端服务器。

---

# 一、项目目标

开发一个基于浏览器的横版1v1 PVP轻量格斗游戏。

玩家打开网站后可以：

1. 注册或登录账号；
2. 上传一张包含人物的照片；
3. 从照片中提取人物身体部位；
4. 自动生成可以操控的格斗角色；
5. 预览角色的移动、跳跃、出拳和扫腿动作；
6. 进入匹配队列；
7. 匹配到另一名玩家；
8. 观看双方简短的入场动画；
9. 开始实时横版格斗；
10. 玩家可以随时退出；
11. 一方退出后，另一方也自动离开竞技场；
12. 双方返回游戏大厅后可以继续匹配。

游戏不设置血条、不判断输赢、不设置回合时间。玩家可以持续对打，直到任意一方退出。

这是一个轻松、搞笑、强调照片角色和打击感的格斗游戏，不包含血腥、肢解和真实伤害表现。

---

# 二、技术栈

使用以下技术栈：

## 前端

* React
* TypeScript
* Vite
* Phaser 3负责格斗场景、动画、物理碰撞和游戏循环
* React负责登录、角色编辑、匹配和菜单界面
* Zustand或其他轻量状态管理方案
* Tailwind CSS负责界面样式
* Canvas API负责人物部位编辑
* Web Audio API或本地无版权音频文件负责音效
* Supabase JavaScript SDK

## 后端

所有业务后端服务使用Supabase：

* Supabase Auth：注册、登录和会话管理
* Supabase PostgreSQL：玩家、角色、匹配和房间数据
* Supabase Storage：保存原始照片和透明角色部件
* Supabase Realtime Broadcast：传输格斗输入与同步消息
* Supabase Presence：判断玩家在线、进入、掉线和退出
* Supabase Edge Functions：匹配、房间创建、退出处理和必要的数据校验
* PostgreSQL RPC：执行需要事务和原子性的匹配操作

不创建独立的Node.js、Go、Java、Python或其他常驻后端服务器。

前端可以部署到静态网站托管服务，但所有账号、数据库、文件、匹配和实时通信功能必须由Supabase提供。

---

# 三、整体页面结构

至少实现以下页面或界面状态：

## 1. 登录注册页

功能包括：

* 游戏Logo和名称
* 邮箱注册
* 邮箱密码登录
* 退出登录
* 忘记密码入口
* 表单校验
* 注册成功提示
* 邮箱验证提示
* 加载状态
* 错误信息提示
* 保持登录状态

登录成功后进入游戏大厅。

不要把Supabase Secret Key或service_role Key放在前端。前端只能使用Project URL和Publishable Key。

## 2. 游戏大厅

大厅展示：

* 玩家昵称
* 玩家头像
* 当前选中的格斗角色
* “创建角色”按钮
* “我的角色”按钮
* “开始匹配”按钮
* “退出登录”按钮
* 当前在线状态
* 匹配状态提示

如果玩家还没有创建角色，点击“开始匹配”时提示玩家先创建并选择一个角色。

## 3. 角色创建工作室

包含以下步骤：

### 第一步：上传照片

允许上传：

* JPG
* JPEG
* PNG
* WEBP

限制：

* 文件最大10MB
* 图片尺寸过大时在浏览器中压缩
* 上传前重新编码图片，尽量去除EXIF等隐私元数据
* 展示图片预览
* 支持更换图片
* 给出人物拍摄建议：尽量正面站立、四肢展开、背景简单、光线清晰

### 第二步：人物识别与背景去除

优先在浏览器端实现自动人物识别，不把图片发送给第三方AI服务。

推荐流程：

1. 使用浏览器端人物分割模型识别人像；
2. 使用人体姿态关键点识别头部、肩、肘、腕、髋、膝和踝；
3. 根据关键点和人物蒙版估算各身体区域；
4. 自动生成身体部件初始蒙版；
5. 允许用户手动修正。

如果自动识别效果不稳定，必须保留完整的手动编辑方案，不能阻塞角色创建。

### 第三步：身体部位切分

为了支持骨骼式动画，将人物拆分为以下透明图层：

* 头部
* 躯干
* 左上臂
* 左前臂
* 右上臂
* 右前臂
* 左大腿
* 左小腿
* 右大腿
* 右小腿

手和脚可以分别与前臂、小腿合并，降低初版复杂度。

编辑器必须支持：

* 自动推荐部位范围
* 自由画笔添加选区
* 橡皮擦删除选区
* 多边形套索选择
* 调整画笔大小
* 放大和缩小
* 拖动画布
* 撤销
* 重做
* 清空当前选区
* 隐藏或显示其他部位
* 半透明蒙版预览
* 独立查看每个身体部件
* 检查部件是否为空
* 检查身体区域是否有明显重叠
* 保存编辑进度

为了避免动画关节处出现明显缝隙，相邻身体部件的蒙版允许存在少量重叠。

### 第四步：设置关节点

自动根据姿态关键点设置：

* 颈部连接点
* 左右肩关节
* 左右肘关节
* 左右髋关节
* 左右膝关节

允许玩家拖动关节点进行修正。

关节点数据必须转换为部件局部坐标，并保存在角色的rig配置中。

### 第五步：角色预览

显示角色实时预览，并依次测试：

* 待机
* 向左移动
* 向右移动
* 跳跃
* 出拳
* 扫腿
* 受击
* 击倒
* 起身
* 入场动画

角色采用2D骨骼式部件旋转和位移动画，不要求生成传统逐帧精灵图。

如果某个部件缺失或关节点异常，需要明确提示玩家返回修改。

### 第六步：保存角色

玩家输入：

* 角色名称
* 可选角色简介

保存内容包括：

* 原始照片
* 透明身体部件
* 缩略图
* 角色配置
* 各部件尺寸
* 关节点
* 默认缩放比例
* 碰撞体配置
* 动画配置
* 创建时间
* 所属用户

玩家可以创建多个角色，但必须选择一个作为当前出战角色。

---

# 四、角色动画系统

使用可配置的程序化骨骼动画系统，不要将动作参数散落在页面代码中。

每个动作使用独立配置描述：

* 动作名称
* 总时长
* 各关键帧时间
* 每个身体部件的位置
* 每个身体部件的旋转角度
* 缩放
* 缓动函数
* 是否循环
* 可取消时间
* 攻击有效帧
* 动作恢复时间

至少实现以下动作：

## 1. 待机

* 身体轻微上下起伏
* 手臂轻微摆动
* 头部轻微移动
* 循环播放
* 不要完全静止

## 2. 左右移动

* 双腿交替摆动
* 身体略微前倾
* 手臂反向摆动
* 根据移动速度控制动画速度
* 角色朝向移动方向

## 3. 跳跃

包含：

* 起跳蓄力
* 离地
* 空中
* 下落
* 落地缓冲

## 4. 出拳

包含：

* 短暂蓄力
* 手臂快速伸出
* 拳部攻击有效帧
* 击中后的短暂停顿
* 快速收拳
* 动作恢复

## 5. 扫腿

包含：

* 身体重心降低
* 支撑腿弯曲
* 攻击腿向前扫动
* 脚部攻击有效帧
* 动作结束后恢复站立

扫腿攻击距离比出拳更远，但动作启动和恢复更慢。

## 6. 受击

根据攻击方向表现：

* 身体后仰
* 头部甩动
* 手臂惯性摆动
* 角色短暂僵直
* 向后滑动

## 7. 击倒和起身

当连续受到较大击退或满足击倒条件时：

* 角色向后倒下
* 短暂躺地
* 自动起身
* 起身期间短暂无敌
* 起身后恢复控制

由于没有血量系统，击倒只作为视觉反馈，不代表游戏结束。

## 8. 入场动画

双方从竞技场左右两侧进入：

* 左侧玩家从左侧走入
* 右侧玩家从右侧走入
* 到达指定位置
* 播放简单摆姿势动作
* 显示角色名称
* 屏幕中央依次显示“READY”和“FIGHT”
* 倒计时结束后开放操作

入场动画控制在3～5秒。

---

# 五、游戏操作

桌面端默认键位：

* `A`：向左移动
* `D`：向右移动
* `W`或`Space`：跳跃
* `J`：出拳
* `K`：扫腿
* `Esc`：打开退出确认界面

需要提供按键说明。

输入系统应支持：

* 同时按键
* 按键按下
* 按键持续
* 按键释放
* 输入缓冲
* 动作冷却
* 攻击期间限制部分移动
* 受击期间暂时禁止操作
* 页面失去焦点时清空输入状态，避免角色持续移动

初版以桌面浏览器为主。代码结构应为以后增加手机虚拟按键和手柄控制预留接口。

---

# 六、竞技场和物理系统

创建一个横版2D竞技场：

* 浏览器横屏布局
* 固定地面
* 左右边界
* 简单但有层次的背景
* 地面阴影
* 双方角色始终保持在镜头范围内
* 根据双方距离轻微调整镜头位置和缩放
* 不允许角色走出场景
* 双方角色不能完全重叠穿过

初版不加入平台、复杂地形和场景机关。

游戏逻辑以固定时间步运行，目标渲染帧率为60 FPS。

## 碰撞体分类

不要直接使用照片透明像素进行碰撞检测，需要建立简单、稳定的碰撞体。

至少包括：

* 主身体碰撞体
* 头部受击框
* 躯干受击框
* 腿部受击框
* 拳部攻击框
* 扫腿攻击框
* 地面检测区域

攻击框只在攻击动作的有效帧中启用。

一次攻击动作对同一目标只能造成一次有效命中，避免每帧重复触发。

---

# 七、无血条格斗机制

游戏不显示血条，也不计算传统生命值。

每次攻击命中后产生：

* 受击僵直
* 击退
* 少量累计冲击值
* 清晰的视觉和声音反馈

可以在内部维护一个不显示给玩家的短期冲击值，用于决定击退和击倒效果：

* 冲击值随时间自动下降
* 连续被攻击时冲击值增加
* 冲击值越高，击退距离越大
* 达到阈值时触发击倒
* 起身后重置或明显降低冲击值

该数值只服务于格斗反馈，不作为胜负或生命值。

避免形成无限连击：

* 受击后设置短暂保护
* 击倒后起身有短暂无敌
* 连续攻击逐步增加击退
* 角色被逼到边缘时给予适当脱离空间

---

# 八、打击反馈

打击反馈是项目重点，不能只做简单碰撞和位移。

每次有效命中至少组合以下效果：

## 视觉反馈

* 40～90毫秒Hit Stop，双方动画短暂停顿
* 轻微屏幕震动
* 被击角色快速闪白或闪亮
* 命中位置出现冲击粒子
* 根据拳击或扫腿使用不同粒子形状
* 少量速度线
* 被击角色产生明显后仰
* 攻击者动作有适度跟进
* 重击时增加镜头冲击
* 落地时产生灰尘粒子

不要使用血液或伤口效果。

## 声音反馈

至少制作或集成：

* 按钮点击音效
* 匹配开始音效
* 匹配成功提示音
* 入场音效
* READY提示音
* FIGHT提示音
* 出拳挥空声
* 出拳命中声
* 扫腿挥空声
* 扫腿命中声
* 跳跃声
* 落地声
* 受击声
* 击倒声
* 退出提示音

不同攻击音效需要有明显区别。

对同一种命中声音随机调整少量音调和音量，避免连续攻击时机械重复。

浏览器通常要求用户交互后才能启动声音，因此第一次点击进入游戏时初始化AudioContext。

提供：

* 总音量控制
* 音效开关
* 设置持久化

使用原创、程序生成或明确可商用的音效，禁止直接使用受版权保护的游戏音效。

---

# 九、实时匹配系统

## 匹配流程

1. 玩家点击“开始匹配”；
2. 检查登录状态；
3. 检查是否选择了有效角色；
4. 将玩家加入匹配队列；
5. 显示匹配中状态和等待时间；
6. 玩家可以取消匹配；
7. 系统寻找另一名等待中的玩家；
8. 通过PostgreSQL事务、RPC或Edge Function原子地创建比赛；
9. 防止同一玩家同时进入多个比赛；
10. 匹配成功后双方收到同一个match ID；
11. 双方加载角色资源；
12. 加入私有Realtime Channel；
13. 双方资源加载完成后进入竞技场；
14. 播放同步入场动画；
15. 开始战斗。

匹配操作必须避免两个客户端同时选择同一名对手的竞态问题。

## 匹配状态

至少包括：

* idle
* queued
* matched
* loading
* ready
* active
* leaving
* ended
* disconnected

## 超时处理

* 匹配等待超过合理时间时继续等待，但允许取消
* 匹配后对手长时间未进入房间，则取消该场比赛
* 角色资源加载失败时显示重试
* 网络断开时显示重连提示
* 超过重连时限后结束比赛
* 清理过期匹配队列记录
* 清理没有进入竞技场的失效比赛

---

# 十、Supabase实时对战方案

由于不使用独立常驻战斗服务器，初版采用Supabase Realtime进行轻量实时同步。

## Channel设计

每场比赛建立私有频道：

```text
match:{matchId}
```

频道只允许该场比赛的两名玩家加入、发送和接收消息。

使用：

* Broadcast传输高频输入和战斗同步消息
* Presence传输玩家进入、准备、在线、离开和掉线状态

不要使用Presence高频同步角色位置。

## 消息类型

定义统一的TypeScript类型，至少包括：

* player_joined
* assets_loaded
* player_ready
* countdown_start
* input
* state_snapshot
* state_hash
* ping
* pong
* player_exit
* match_end
* rematch_request
* error

每条消息至少携带：

* matchId
* playerId
* sequence
* timestamp
* messageType
* payload

## 初版同步策略

采用“输入同步＋临时房主状态校正”的方式：

1. 比赛创建者或规则选定的一方作为临时Host；
2. 两端都运行本地游戏模拟；
3. 玩家发送按键输入，而不是每帧上传完整角色数据；
4. 输入消息包含帧号和递增序号；
5. Host负责确定主要碰撞、命中、击退和击倒结果；
6. Host定期发送精简状态快照；
7. 非Host客户端进行本地预测；
8. 状态差异较小时平滑校正；
9. 状态差异较大时快速同步到Host状态；
10. 使用ping/pong估算网络延迟；
11. 丢弃重复或明显过期的输入；
12. 对短时间乱序消息进行排序；
13. 不把高频战斗数据逐帧写入PostgreSQL。

状态快照可以包含：

* 当前模拟帧
* 两名玩家的位置
* 速度
* 朝向
* 当前动作
* 动作进度
* 是否在地面
* 受击状态
* 冲击值
* 最近一次有效命中编号

为后续升级真正的帧同步或Rollback Netcode保留抽象接口。

需要在README中明确说明：该Supabase-only方案适合原型和轻度娱乐对战，不提供专用权威战斗服务器级别的反作弊能力。

---

# 十一、退出和掉线机制

竞技场右上角必须有明显但不遮挡游戏的“退出”按钮。

点击后：

1. 弹出确认框；
2. 确认后向频道发送player_exit；
3. 将比赛状态更新为ended；
4. 当前玩家退出竞技场；
5. 另一名玩家收到消息后也退出竞技场；
6. 双方返回游戏大厅；
7. 清理频道订阅和本地游戏状态；
8. 玩家可以再次点击匹配。

如果玩家：

* 关闭网页
* 刷新网页
* 浏览器崩溃
* 网络断开
* Presence离开
* 长时间没有心跳

另一名玩家需要看到“对方已离开”或“对方连接中断”的提示，然后自动返回大厅。

不要让一方退出后另一方永久停留在空竞技场。

返回大厅时必须完整销毁：

* Phaser场景
* 键盘监听
* Realtime Channel
* Presence订阅
* 定时器
* 音频实例
* 匹配状态
* 未完成的网络请求

避免重复匹配后产生多重监听和重复消息。

---

# 十二、数据库设计

生成完整的Supabase SQL migration，至少创建以下表。

## profiles

字段建议：

* id，UUID，关联auth.users
* username
* display_name
* avatar_url
* created_at
* updated_at

## fighters

字段建议：

* id
* owner_id
* name
* description
* thumbnail_path
* original_image_path
* rig_manifest，JSONB
* animation_config，JSONB
* collider_config，JSONB
* is_ready
* created_at
* updated_at

## fighter_parts

字段建议：

* id
* fighter_id
* part_type
* storage_path
* width
* height
* pivot_x
* pivot_y
* sort_order
* metadata，JSONB

## player_settings

字段建议：

* user_id
* selected_fighter_id
* master_volume
* sound_enabled
* control_config，JSONB
* updated_at

## matchmaking_queue

字段建议：

* id
* player_id
* fighter_id
* status
* region
* created_at
* heartbeat_at

同一玩家只能存在一条有效排队记录。

## matches

字段建议：

* id
* player_one_id
* player_two_id
* player_one_fighter_id
* player_two_fighter_id
* host_player_id
* status
* created_at
* started_at
* ended_at
* ended_by
* end_reason

## match_players

字段建议：

* match_id
* player_id
* fighter_id
* joined_at
* ready_at
* left_at
* connection_status

为常用查询添加索引、唯一约束、外键和必要的级联删除规则。

---

# 十三、Storage设计

建立以下Bucket或等价目录结构：

```text
fighter-originals/{userId}/{fighterId}/original.webp
fighter-parts/{userId}/{fighterId}/head.webp
fighter-parts/{userId}/{fighterId}/torso.webp
fighter-parts/{userId}/{fighterId}/left-upper-arm.webp
fighter-parts/{userId}/{fighterId}/left-lower-arm.webp
fighter-parts/{userId}/{fighterId}/right-upper-arm.webp
fighter-parts/{userId}/{fighterId}/right-lower-arm.webp
fighter-parts/{userId}/{fighterId}/left-upper-leg.webp
fighter-parts/{userId}/{fighterId}/left-lower-leg.webp
fighter-parts/{userId}/{fighterId}/right-upper-leg.webp
fighter-parts/{userId}/{fighterId}/right-lower-leg.webp
fighter-thumbnails/{userId}/{fighterId}/thumbnail.webp
```

要求：

* 原始照片设为私有
* 角色部件默认设为私有
* 玩家只能修改或删除自己的文件
* 比赛期间允许参赛双方读取对方选中的角色资源
* 可通过短期Signed URL加载对手角色
* Signed URL有效期应覆盖一场正常比赛
* 文件路径不能由客户端随意冒充其他用户
* 删除角色时同步删除相关Storage文件

---

# 十四、RLS安全策略

为所有暴露给客户端的表启用Row Level Security。

至少实现以下策略：

* 玩家只能修改自己的profile
* 玩家只能创建、修改和删除自己的fighter
* 玩家只能管理自己的角色部件
* 玩家只能修改自己的设置
* 玩家只能加入或取消自己的匹配队列
* 玩家只能读取自己参与的比赛
* 玩家只能更新自己对应的match_players记录
* 比赛双方可以读取该场比赛需要的数据
* 非参赛玩家不能加入比赛私有频道
* 只有比赛双方能向对应Realtime Channel发送消息
* 玩家不能把自己加入不属于自己的比赛
* 玩家不能直接修改对手数据
* 客户端不能使用service_role权限

生成完整、可执行的SQL策略，不要只写伪代码。

---

# 十五、界面与视觉风格

整体风格：

* 街机格斗
* 简洁
* 有活力
* 略带夸张和幽默感
* 深色背景配高饱和强调色
* 清晰的大按钮
* 适合桌面横屏
* 不使用复杂写实UI

大厅、角色工作室和竞技场需要保持统一视觉语言。

竞技场UI只显示：

* 双方玩家名称
* 双方角色名称
* 网络延迟
* 音效按钮
* 退出按钮
* 必要的连接状态提示

不要显示血条、胜负、倒计时或比分。

所有按钮必须有：

* Hover状态
* Pressed状态
* Disabled状态
* Loading状态
* 错误反馈

适配常见桌面分辨率，最低兼容1366×768。窗口过窄时提示用户横屏或扩大窗口。

---

# 十六、异常处理

必须处理以下情况：

* 未登录访问大厅
* 未创建角色开始匹配
* 图片格式错误
* 图片超过大小限制
* 人物识别失败
* 某个身体部件为空
* 关节点位置异常
* Storage上传失败
* 保存角色失败
* Supabase断线
* Realtime频道加入失败
* 匹配超时
* 对手未加载完成
* 对手刷新页面
* 对手主动退出
* 对手异常掉线
* 重复点击匹配
* 同一账号打开多个页面
* 游戏页面失去焦点
* 浏览器不支持相关图像处理能力
* 音频无法自动播放
* 已过期的Signed URL
* 重复订阅Realtime Channel
* 退出后资源没有释放

错误信息必须对普通用户可读，不能只显示技术错误码。

同时在开发环境保留足够的日志，方便定位网络和匹配问题。

---

# 十七、代码结构

请按模块拆分，不要把全部功能写进一个React组件。

建议结构：

```text
src/
  app/
  components/
  pages/
  features/
    auth/
    lobby/
    fighter-editor/
    matchmaking/
    arena/
  game/
    scenes/
    entities/
    animation/
    combat/
    collision/
    input/
    networking/
    audio/
    effects/
    config/
  lib/
    supabase/
    image-processing/
    storage/
  stores/
  types/
  utils/
supabase/
  migrations/
  functions/
  seed.sql
```

需要定义清晰接口：

* FighterManifest
* FighterPart
* RigJoint
* AnimationDefinition
* ColliderDefinition
* CombatInput
* PlayerState
* MatchState
* RealtimeMessage
* MatchmakingStatus

图像处理、角色动画、格斗逻辑和网络通信必须相互解耦。

将Supabase访问封装在独立service中，不要让所有组件直接操作数据库。

---

# 十八、测试要求

至少增加以下测试：

## 单元测试

* 动作状态切换
* 攻击有效帧
* 一次攻击只命中一次
* 冲击值衰减
* 击倒与起身
* 输入序号排序
* 重复输入丢弃
* 状态快照解析
* 角色manifest校验

## 集成测试

* 注册并登录
* 创建角色
* 上传身体部件
* 选择出战角色
* 加入和取消匹配
* 两个玩家成功匹配
* 双方加入同一频道
* 一方退出后另一方返回大厅
* 掉线后比赛正确结束

## 手动测试页面

提供开发模式下的双玩家测试方法：

* 在两个浏览器窗口中登录不同账号
* 快速生成测试角色
* 进行本地或测试环境匹配
* 显示网络消息调试面板
* 可模拟延迟、丢包和断线
* 可查看碰撞框和攻击框
* 可查看当前动作、帧号和状态快照

生产环境必须关闭碰撞框和网络调试信息。

---

# 十九、开发顺序

请按照以下顺序实现，并确保每个阶段都可以运行：

## 阶段1：基础工程

* 创建React、TypeScript、Vite和Phaser项目
* 配置Supabase客户端
* 配置环境变量
* 建立路由和基础视觉样式

## 阶段2：账号系统

* 注册
* 登录
* 会话恢复
* profile创建
* 登出
* 路由保护

## 阶段3：角色编辑器

* 上传照片
* 人物自动识别
* 手动画笔和套索修正
* 身体部位导出
* 关节点编辑
* 角色预览
* Storage上传
* 数据库存储

## 阶段4：本地格斗模块

* 竞技场
* 输入
* 动画
* 碰撞
* 出拳
* 扫腿
* 跳跃
* 受击
* 击退
* 击倒
* 起身
* 打击反馈
* 音效

先确保两个本地测试角色可以在同一个浏览器中正常格斗。

## 阶段5：匹配系统

* 匹配队列
* 原子匹配
* 创建比赛
* 取消匹配
* 超时清理
* 进入竞技场

## 阶段6：实时联机

* 私有频道
* Presence
* Broadcast
* 输入同步
* Host快照
* 延迟显示
* 状态校正
* 退出和掉线处理

## 阶段7：完善与测试

* 异常处理
* 加载性能
* 资源释放
* RLS检查
* 多窗口测试
* 部署说明
* README

不要跳过前面的本地格斗验证，直接开发联机，否则很难区分格斗逻辑问题和网络同步问题。

---

# 二十、交付内容

最终必须提供：

1. 完整前端源代码；
2. 完整Supabase SQL migration；
3. 完整RLS策略；
4. Storage Bucket和权限配置说明；
5. Edge Functions代码；
6. 环境变量示例文件；
7. 本地启动命令；
8. Supabase项目配置步骤；
9. 部署步骤；
10. 双账号测试方法；
11. 项目架构说明；
12. 网络同步原理说明；
13. 已知限制说明；
14. 后续升级专用战斗服务器的接口预留说明。

提供 `.env.example`：

```env
VITE_SUPABASE_URL=
VITE_SUPABASE_PUBLISHABLE_KEY=
```

不要在代码库中提交真实密钥。

README必须让一个没有参与开发的人按照步骤完成：

* 安装依赖
* 创建Supabase项目
* 执行migration
* 创建Storage Bucket
* 配置Auth
* 填写环境变量
* 启动项目
* 创建两个测试账号
* 创建测试角色
* 完成一次1v1匹配

---

# 二十一、验收标准

项目达到以下条件才算完成：

* 用户可以注册并登录
* 会话刷新后不会丢失
* 用户可以上传人物照片
* 自动识别失败时可以手动划分身体部位
* 可以保存至少一个完整角色
* 角色能够播放待机、移动、跳跃、出拳、扫腿、受击、击倒和起身动作
* 攻击具有有效碰撞判定
* 攻击反馈明显
* 两个真实账号可以进入匹配
* 双方能够加入同一场比赛
* 双方能看到彼此角色
* 双方操作能够通过Supabase Realtime同步
* 一方退出后另一方自动返回大厅
* 掉线后不会让比赛永久卡住
* 返回大厅后可以再次匹配
* 不显示血条
* 不设置胜负
* 游戏有完整音效
* 所有表启用合理的RLS
* 前端不存在Secret Key或service_role Key
* 没有依赖Supabase以外的自建后端
* 项目可以按照README从零部署
* 代码中不保留影响核心流程的TODO、假数据或未实现按钮

开发过程中如果某项自动人物分割能力难以稳定实现，不要删除角色创建功能，应立即采用“自动粗分割＋用户手动画笔修正”的可运行方案。

优先交付一个完整、稳定、真正可以双人游玩的MVP，再考虑增加复杂连招、更多攻击动作、排行榜、好友邀请、表情、观战和角色装饰系统。
# 二十二、黑客松现场展示与扫码访问

本项目用于黑客松比赛，开发和部署时间有限。必须优先保证评委和现场观众能够通过手机扫码快速进入游戏。

不要求购买自定义域名。需要同时支持以下两种演示方式。

## 方案A：公网部署，作为主要展示方案

将前端构建为静态网站，部署到支持HTTPS的前端托管平台。

可以使用平台自动分配的免费二级域名，例如：

```text
https://project-name.vercel.app
```

不要求购买或配置自定义域名。

所有后端功能仍然使用Supabase。前端托管平台只负责分发网页文件，不承担数据库、认证、匹配或战斗后端功能。

部署后需要：

* 将正式访问地址配置为Supabase Auth的Site URL
* 将正式访问地址加入Supabase Auth的Redirect URLs
* 检查注册、登录和密码重置跳转
* 检查手机浏览器是否能正常打开
* 检查手机网络下能否连接Supabase
* 检查HTTPS环境下的图片上传、音频和Realtime连接
* 在比赛前准备好最终访问二维码

公网部署是黑客松正式展示的推荐方式，因为它不依赖现场Wi-Fi是否允许设备互访。

## 方案B：同一Wi-Fi局域网访问，作为备用方案

允许在演示电脑上运行前端，并向同一局域网中的手机开放。

配置Vite：

```ts
export default defineConfig({
  server: {
    host: '0.0.0.0',
    port: 5173
  },
  preview: {
    host: '0.0.0.0',
    port: 4173
  }
})
```

提供以下脚本：

```json
{
  "scripts": {
    "dev:lan": "vite --host 0.0.0.0 --port 5173",
    "demo:build": "vite build",
    "demo:lan": "vite preview --host 0.0.0.0 --port 4173"
  }
}
```

局域网正式演示优先使用：

```bash
npm run demo:build
npm run demo:lan
```

其他设备通过演示电脑的局域网IP访问：

```text
http://演示电脑局域网IP:4173
```

例如：

```text
http://192.168.1.25:4173
```

README中说明如何查看电脑局域网IP：

* Windows：运行`ipconfig`
* macOS：在网络设置中查看IP，或运行`ipconfig getifaddr en0`
* Linux：运行`hostname -I`

同时提示检查：

* 手机和电脑是否连接同一个Wi-Fi
* 两台设备是否处于同一网段
* 系统防火墙是否允许Node/Vite访问网络
* 路由器是否开启AP Isolation或客户端隔离
* 网络是否可以访问Supabase云服务
* 端口5173或4173是否被占用
* VPN是否影响局域网访问

如果现场Wi-Fi开启客户端隔离，局域网访问可能失败，此时立即使用公网部署地址。

Supabase是云服务，因此即使网页通过局域网访问，现场网络通常仍需能够访问互联网。

## 现场二维码入口

在游戏首页增加“现场试玩”区域，显示当前网站地址对应的二维码。

二维码必须根据以下地址动态生成：

```ts
window.location.origin
```

这样：

* 公网部署时，二维码自动指向公网地址
* 局域网演示时，二维码自动指向局域网IP地址
* 更换部署地址后不需要手动重新修改二维码内容

注意：如果演示电脑使用`localhost`打开页面，不能直接使用`localhost`生成二维码，因为观众手机中的`localhost`代表观众自己的手机。

局域网演示时必须通过电脑的实际局域网IP打开管理页面，再生成二维码。

二维码界面需要提供：

* 大尺寸二维码
* 当前访问地址
* “复制链接”按钮
* “全屏展示二维码”按钮
* “刷新二维码”按钮
* 连接状态提示
* 简短说明：“连接现场Wi-Fi后扫码进入”
* 备用短链接或纯文本地址

二维码需要保持足够的留白和对比度，避免添加过多图案影响识别。

## 黑客松快速试玩模式

正式账号注册流程保留，但现场展示不能强制每位观众填写邮箱、验证邮件并制作完整角色。

在登录页增加两个入口：

1. “注册/登录”
2. “游客快速试玩”

游客快速试玩使用Supabase Anonymous Sign-In：

```ts
const { data, error } = await supabase.auth.signInAnonymously()
```

匿名用户也必须获得独立的Supabase用户ID和会话，不能让所有游客共享同一个测试账号。

首次进入时只要求游客填写：

* 临时昵称
* 从预设角色中选择一个角色

完成后即可进入大厅和匹配队列，整个过程尽量控制在10～20秒。

匿名玩家关闭浏览器、清除数据或退出后，不保证能够恢复原账号。界面中应简短提示：

```text
游客数据仅用于现场试玩，关闭浏览器后可能无法恢复。
```

## 预设演示角色

为了避免观众必须现场上传照片和切分身体部件才能体验游戏，项目必须内置至少4个已经配置完成的演示角色。

预设角色需要：

* 具有不同外观
* 能正常播放全部动作
* 具有完整碰撞体
* 可以直接用于匹配
* 不使用有版权风险的人物照片
* 可以使用原创插画、开发者授权照片或简单几何角色

游客仍然可以选择进入完整的照片角色创建流程，但这不是开始试玩的必要条件。

正式注册用户可以：

* 使用预设角色
* 上传照片创建自己的角色
* 保存多个自定义角色

## 现场演示首页

增加专门的Hackathon Demo入口页面，展示：

* 游戏名称和一句话介绍
* “游客快速试玩”
* “注册/登录”
* “观看操作演示”
* 操作按键说明
* 当前在线人数
* 当前匹配人数
* 大尺寸二维码
* 网络连接状态
* Supabase连接状态
* Realtime连接状态

页面应让第一次接触游戏的观众在没有工作人员指导的情况下完成进入、选择角色和开始匹配。

## 手机访问与操作限制

当前格斗操作主要针对桌面键盘，但扫码进入的观众大概率使用手机，因此必须实现以下两种方案之一。

优先方案：实现手机横屏虚拟按键。

左侧：

* 向左
* 向右

右侧：

* 跳跃
* 出拳
* 扫腿

手机端要求：

* 提示用户横屏
* 使用大尺寸触控按钮
* 禁止按钮文字被长按选中
* 防止页面双击缩放
* 防止触控过程中页面滚动
* 支持多点触控
* 按钮按下时有明显视觉反馈
* 保留安全边距，避免被系统手势区域遮挡
* 游戏画面适配常见手机横屏比例

如果黑客松时间不足以实现可靠的手机格斗操作，则扫码页面必须明确区分：

* “手机：查看项目和创建角色”
* “电脑：进行完整格斗试玩”

不能让观众进入竞技场后才发现手机无法操作。

## 现场快速匹配

为演示环境增加“快速匹配”模式：

* 游客选择角色后直接进入队列
* 不展示复杂设置
* 优先匹配同一演示区域的玩家
* 同一时间仍然是1v1比赛
* 排队期间显示等待人数
* 允许随时取消
* 匹配成功后自动加载竞技场
* 一方退出后双方返回快速匹配首页
* 返回后可以立即再次匹配

可以通过环境变量区分普通模式和黑客松展示模式：

```env
VITE_DEMO_MODE=true
VITE_PUBLIC_SITE_URL=
VITE_DEMO_REGION=hackathon
```

展示模式下：

* 默认突出游客快速试玩
* 默认显示二维码
* 默认使用预设角色
* 简化注册入口
* 显示现场网络诊断
* 不改变正式账号和自定义角色功能

## 演示连接自检

增加一个仅在Demo Mode显示的网络诊断面板，自动检查：

* 当前页面地址
* 是否使用HTTPS
* 是否成功连接Supabase
* Auth是否可用
* Database是否可读
* Storage是否可访问
* Realtime WebSocket是否连接
* 当前网络延迟
* 当前用户ID
* 当前是否为匿名用户
* 当前比赛频道状态

每项使用清晰状态显示：

* 正常
* 检查中
* 异常

连接失败时给出可执行提示，而不是只显示错误代码。

## 匿名用户安全限制

匿名用户虽然是临时玩家，但仍然需要受到RLS约束。

限制匿名用户：

* 只能修改自己的临时profile
* 只能加入自己的匹配队列
* 只能访问自己参与的比赛
* 只能加入自己比赛对应的Realtime频道
* 不能修改其他玩家角色
* 不能读取原始私人照片
* 不能执行管理员操作
* 不能无限上传文件
* 不能创建大量垃圾匹配记录

如果允许匿名用户上传照片：

* 单张图片最大5MB
* 每个匿名用户最多创建1个临时角色
* 限制上传频率
* 临时资源标记过期时间
* 使用Supabase定时任务清理过期匿名数据

如果时间不足，匿名用户只允许使用预设角色，注册用户才可以上传照片。

## 黑客松演示准备清单

在README中增加“比赛现场展示”章节，列出：

### 比赛前一天

* 完成公网部署
* 固定Supabase生产项目
* 配置Site URL和Redirect URLs
* 启用匿名登录
* 准备至少4个预设角色
* 创建两个正式测试账号
* 测试两个手机之间匹配
* 测试电脑与手机匹配
* 打印或保存二维码
* 准备局域网备用方案
* 将所有依赖提前安装
* 保存可离线启动的前端构建文件

### 到达现场后

* 测试现场Wi-Fi
* 测试公网地址
* 测试Supabase连接
* 测试Realtime连接
* 检查手机横屏操作
* 检查音效
* 检查二维码扫描
* 测试局域网设备互访
* 关闭电脑睡眠和自动锁屏
* 接通电源
* 关闭可能影响网络的VPN
* 准备手机热点作为备用网络

### 正式展示时

* 演示电脑提前打开大厅
* 投影显示二维码
* 使用预设角色缩短流程
* 让两名观众扫码进入
* 游客输入昵称
* 选择预设角色
* 自动完成1v1匹配
* 展示照片角色制作流程
* 展示打击反馈和实时联机
* 展示一方退出后双方同步返回大厅

## 验收标准补充

现场展示功能达到以下条件才算完成：

* 不购买自定义域名也能运行
* 公网部署地址可以直接访问
* 同一Wi-Fi下可以通过局域网IP访问备用版本
* 二维码始终指向其他设备可访问的地址
* 不能生成指向localhost的现场二维码
* 手机扫码后可以正常打开页面
* 游客不需要邮箱即可快速进入
* 匿名用户具有独立身份
* 游客可以直接使用预设角色
* 两名游客可以完成一次1v1匹配
* 手机端可以操作，或者在进入前明确提示仅支持电脑操作
* 一方退出后双方正确返回
* 现场网络异常时有清晰诊断信息
* README包含完整的黑客松部署和检查流程

实现时优先级如下：

1. 公网地址能够稳定打开；
2. 二维码能够正常扫描；
3. 游客快速登录；
4. 预设角色快速试玩；
5. 两人实时匹配；
6. 局域网备用访问；
7. 手机触控操作；
8. 网络诊断面板；
9. 观战等扩展功能。

在黑客松时间有限的情况下，不要为了自定义域名、复杂注册流程或高级观战系统影响核心双人试玩功能。
# 二十三、好友与交流系统

增加完整的好友和私聊功能。

玩家可以：

* 通过用户名搜索其他玩家
* 发送好友申请
* 接受或拒绝好友申请
* 查看好友列表
* 查看好友在线状态
* 与好友进行文字聊天
* 删除好友
* 屏蔽用户
* 从好友资料页邀请对方进行1v1匹配
* 与同一名玩家格斗超过一分钟后自动成为好友

## 用户名规则

每个正式账号必须设置唯一用户名。

用户名要求：

* 长度为3～20个字符
* 支持中文、英文字母、数字和下划线
* 不允许只有空格
* 不允许使用系统保留名称
* 不区分英文字母大小写
* 数据库中使用大小写不敏感的唯一约束
* 用户可以另外设置允许重复的显示名称

玩家搜索时使用用户名，不使用邮箱、手机号或Supabase用户ID。

搜索结果只展示：

* 用户名
* 显示名称
* 用户头像
* 当前使用的格斗角色缩略图
* 在线状态
* 好友关系状态

不要公开邮箱、真实姓名、原始照片路径或其他隐私信息。

搜索需要：

* 输入至少2个字符后开始查询
* 使用300～500毫秒防抖
* 每次最多返回20条结果
* 支持精确搜索和前缀搜索
* 不显示已经屏蔽当前玩家的用户
* 不显示当前玩家自己
* 避免客户端短时间内发送大量搜索请求

## 手动添加好友

玩家在搜索结果或玩家资料卡中点击“添加好友”。

好友申请状态包括：

* none
* pending_sent
* pending_received
* accepted
* rejected
* blocked

好友申请流程：

1. 玩家A搜索玩家B；
2. 玩家A点击“添加好友”；
3. 系统创建好友申请；
4. 玩家B收到实时通知；
5. 玩家B可以接受或拒绝；
6. 接受后双方进入好友列表；
7. 双方可以开始私聊；
8. 重复点击不能创建多条相同申请。

如果双方已经是好友，按钮显示“已是好友”。

如果对方已经向当前玩家发送申请，按钮显示“接受申请”，不再创建反向申请。

好友关系必须是双向的，不能出现A的好友列表中有B，但B的好友列表中没有A的情况。

## 对战满一分钟自动加好友

如果两名玩家在同一场1v1比赛中的有效格斗时间达到或超过60秒，系统自动将双方添加为好友，不需要再次发送申请。

有效格斗时间从以下时刻开始计算：

* 双方角色资源加载完成
* 双方进入竞技场
* READY和FIGHT动画结束
* 比赛状态正式变为active

以下时间不计入：

* 匹配等待
* 角色资源加载
* 入场动画
* 暂停状态
* 长时间断线状态
* 比赛结束后的停留时间

自动添加好友流程：

1. 比赛正式开始时，由数据库记录`started_at`；
2. 双方持续格斗达到60秒；
3. 客户端调用Supabase RPC或Edge Function；
4. 服务端验证调用者确实是该比赛参与者；
5. 服务端验证比赛状态仍为active；
6. 服务端验证`started_at`距离当前时间不少于60秒；
7. 服务端验证双方没有互相屏蔽；
8. 原子创建双向好友关系；
9. 如果双方已经是好友，则不重复创建；
10. 双方收到“你们已经一起格斗超过1分钟，已自动成为好友”的提示；
11. 好友关系创建后立即开放私聊入口。

不能只依赖客户端倒计时判断满一分钟。客户端只能发起请求，最终必须由Supabase数据库时间和比赛记录进行验证，避免客户端修改时间后提前添加好友。

自动添加操作必须是幂等的。即使双方客户端同时触发，数据库也只能生成一条好友关系。

如果任意一方在60秒前退出，则不自动添加好友。

如果达到60秒后再退出，已经建立的好友关系继续保留。

如果双方已经存在未处理的好友申请，自动添加好友时：

* 直接将关系更新为accepted
* 自动关闭对应的待处理申请
* 不产生重复记录

如果双方存在屏蔽关系，则禁止自动添加好友。

好友关系记录需要保存来源：

* manual_request：通过搜索和申请添加
* match_auto：格斗满一分钟自动添加
* admin：系统管理操作

自动好友关系还需要保存来源比赛的`match_id`。

## 自动加好友设置

在玩家设置中增加：

```text
格斗满1分钟后自动添加对手为好友
```

默认开启。

当任意一方关闭该设置时，比赛满一分钟后不直接建立好友关系，改为向双方展示：

```text
你们已经格斗超过1分钟，是否添加对方为好友？
```

这样既保留自动加好友功能，也允许玩家控制社交偏好。

黑客松Demo Mode可以默认开启自动添加好友，以便现场快速展示完整社交闭环。

## 好友列表

游戏大厅增加“好友”入口和未读消息数量。

好友列表展示：

* 头像
* 用户名
* 显示名称
* 当前格斗角色
* 在线状态
* 最后在线时间
* 是否正在匹配
* 是否正在格斗
* 未读消息数量

好友列表支持：

* 在线好友优先
* 搜索已有好友
* 打开聊天
* 查看玩家资料
* 发起对战邀请
* 删除好友
* 屏蔽用户

在线状态通过Supabase Presence维护，但不要高频更新Presence。

## 好友私聊

好友之间可以进行一对一文字聊天。

初版只实现文本消息，不实现图片、语音、视频和文件发送。

聊天功能包括：

* 发送文字
* 实时接收消息
* 查看历史消息
* 未读消息数量
* 消息时间
* 发送中状态
* 发送失败重试
* 自动滚动到最新消息
* 分页加载历史消息
* 标记消息已读
* 删除自己本地视图中的会话入口
* 屏蔽用户

消息限制：

* 单条消息最多500个字符
* 去除首尾空格
* 空消息不能发送
* 限制短时间连续发送
* 对HTML内容进行转义
* 禁止直接渲染用户输入的HTML
* 不允许通过消息执行脚本
* 被屏蔽后双方不能继续发送消息

聊天消息写入PostgreSQL进行持久化，并通过Supabase Realtime订阅新消息。

不要只使用Broadcast传输聊天，否则刷新页面后会丢失消息记录。

## 对战后交流入口

比赛退出后，在返回大厅前短暂展示“本场对手”卡片：

* 对手用户名
* 对手角色
* 本场持续时间
* 当前好友状态
* “发送消息”
* “查看资料”
* “再次对战”
* “返回大厅”

如果格斗已经超过一分钟并自动成为好友，突出显示：

```text
你们已经成为好友，可以继续聊天或再次对战。
```

任意一方主动退出时，双方仍然返回大厅，但已经满足一分钟条件的好友关系不受影响。

## 好友对战邀请

好友可以发送1v1对战邀请。

邀请状态包括：

* pending
* accepted
* rejected
* expired
* cancelled

流程：

1. 玩家A向在线好友B发送邀请；
2. 玩家B实时收到邀请弹窗；
3. 玩家B可以接受或拒绝；
4. 接受后系统创建match；
5. 双方进入同一个竞技场；
6. 邀请超过30秒自动失效；
7. 如果任意一方已经在匹配或格斗，则不能重复邀请。

好友邀请进入的比赛仍然遵循相同的退出、掉线和Realtime规则。

## 数据库表补充

增加以下表。

### friend_requests

字段建议：

* id
* sender_id
* receiver_id
* status
* created_at
* responded_at

约束：

* 不能向自己发送申请
* 同一对用户只能有一条有效待处理申请
* 已经是好友时不能再次申请

### friendships

使用单条记录表示一组双向好友关系。

字段建议：

* id
* user_low_id
* user_high_id
* source
* source_match_id
* created_at

其中：

* `user_low_id`和`user_high_id`按UUID稳定排序
* 两个字段不能相同
* 两个用户组合必须唯一
* 查询任意用户好友时同时检查两个字段

### direct_messages

字段建议：

* id
* sender_id
* receiver_id
* content
* created_at
* read_at
* client_message_id

`client_message_id`用于防止网络重试造成重复消息。

### user_blocks

字段建议：

* blocker_id
* blocked_id
* created_at

屏蔽关系优先级高于好友关系和自动加好友。

### fight_invites

字段建议：

* id
* sender_id
* receiver_id
* status
* created_at
* expires_at
* match_id

### player_settings补充字段

增加：

* auto_friend_after_match，Boolean，默认true
* allow_friend_requests，Boolean，默认true
* allow_fight_invites，Boolean，默认true

### matches补充字段

增加：

* active_duration_seconds
* auto_friend_processed_at

`auto_friend_processed_at`用于防止同一场比赛多次执行自动加好友。

## RPC或Edge Function

实现原子函数：

```text
search_players(query)
send_friend_request(receiver_id)
respond_friend_request(request_id, action)
remove_friend(friend_id)
block_user(target_id)
unblock_user(target_id)
auto_friend_from_match(match_id)
send_fight_invite(receiver_id)
respond_fight_invite(invite_id, action)
```

其中`auto_friend_from_match(match_id)`必须在服务端完成全部资格验证和事务处理。

不要允许客户端直接向`friendships`表插入任意好友关系。

## RLS安全要求

增加以下安全策略：

* 玩家只能代表自己发送好友申请
* 玩家只能查看自己发送或收到的申请
* 只有接收者可以接受或拒绝申请
* 玩家只能查看包含自己的好友关系
* 玩家不能直接伪造自动好友来源
* 玩家只能查看自己参与的私聊消息
* 只有好友才能互相发送私聊消息
* 被屏蔽后不能继续发送消息
* 玩家只能查看与自己有关的对战邀请
* 玩家不能冒充他人发送对战邀请
* 玩家只能管理自己创建的屏蔽关系
* 用户名搜索函数不能返回邮箱和私人字段
* 匿名游客不能无限搜索或发送消息
* service_role和Secret Key不能出现在客户端

## 验收标准补充

好友交流模块达到以下条件才算完成：

* 可以通过用户名搜索玩家
* 用户名具有大小写不敏感的唯一性
* 可以发送、接受和拒绝好友申请
* 不会生成重复好友关系
* 好友关系在双方列表中保持一致
* 好友之间可以实时发送文字消息
* 刷新页面后可以查看历史消息
* 能正确显示未读消息数量
* 可以删除好友
* 可以屏蔽和解除屏蔽用户
* 被屏蔽用户不能发送消息或自动成为好友
* 两名玩家有效格斗达到60秒后自动成为好友
* 60秒由Supabase服务端时间验证
* 双方同时触发不会产生重复数据
* 60秒前退出不会自动成为好友
* 60秒后退出不会删除好友关系
* 自动加好友后双方收到明确提示
* 玩家可以关闭自动加好友设置
* 好友可以发送1v1对战邀请
* 所有好友、消息和邀请数据受到RLS保护
